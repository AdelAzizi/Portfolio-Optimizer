# تحلیل و ارزیابی پروژه `Portfolio-Optimizer`

## 1. ارزیابی کلی (Overall Assessment)

پروژه `Portfolio-Optimizer` یک سیستم تحلیل کمی برای بازار بورس ایران است که به خوبی مراحل کلیدی یک پایپ‌لاین سرمایه‌گذاری را پیاده‌سازی کرده است. ساختار کلی پروژه که شامل مراحل زیر است، منطقی و درست طراحی شده است:

1.  **ایجاد جهان سرمایه‌گذاری (Universe Creation):** انتخاب سهام مناسب برای تحلیل.
2.  **جمع‌آوری داده (Data Collection):** دریافت داده‌های قیمت و بنیادی.
3.  **پیش‌پردازش (Preprocessing):** آماده‌سازی داده‌ها برای تحلیل.
4.  **تست استراتژی (Strategy Testing):** ارزیابی عملکرد استراتژی‌های مختلف.
5.  **اعتبارسنجی و انتخاب نهایی (Validation & Selection):** انتخاب بهترین استراتژی‌ها.

استفاده از کتابخانه‌های قدرتمندی مانند `pandas`, `numpy` و `pytse-client` کاملاً مناسب است. با این حال، از منظر مهندسی نرم‌افزار، پروژه دارای نقاط ضعفی است که با بهبود آن‌ها می‌توان قابلیت نگهداری، توسعه‌پذیری و استحکام آن را به میزان قابل توجهی افزایش داد.

---

## 2. مشکلات شناسایی‌شده و راه‌حل‌های پیشنهادی

در این بخش، مشکلات اصلی پروژه به همراه راه‌حل پیشنهادی و اولویت هرکدام بررسی می‌شود.

### موضوع ۱: مدیریت نامناسب مخزن کد (Git Repository)

-   **مشکل:** فایل‌های تولیدی (Generated Files) مانند داده‌های `csv` و `feather`، فایل‌های لاگ، تصاویر و فایل‌های کش پایتون (`__pycache__`) مستقیماً در مخزن `Git` ذخیره (Track) شده بودند. این کار حجم مخزن را به شدت افزایش داده، همکاری تیمی را دشوار کرده و اصول مدیریت نسخه را نقض می‌کند.
-   **راه‌حل:** یک فایل `.gitignore` جامع برای نادیده گرفتن این نوع فایل‌ها ایجاد شد و فایل‌های موجود از ایندکس `Git` حذف شدند.
-   **وضعیت:** ✅ **انجام شده (Fixed)**
-   **اهمیت:** **بسیار بالا (Critical)**. این مشکل زیربنایی‌ترین مسئله پروژه بود که خوشبختانه برطرف گردید.

### موضوع ۲: پراکندگی تنظیمات و مقادیر ثابت (Scattered & Hardcoded Configurations)

-   **مشکل:** پارامترهای کلیدی مانند مسیر فایل‌ها، آستانه‌های فیلتر کردن سهام (مانند حداقل حجم معاملات یا ارزش بازار) و سایر مقادیر ثابت به جای اینکه در یک فایل مرکزی باشند، در فایل‌های مختلف (`run_pipeline.py`, `universe_creator.py` و ...) به صورت "Hardcode" نوشته شده‌اند. این وضعیت تغییر دادن پارامترها را بسیار دشوار و خطاپذیر می‌کند.
-   **راه‌حل:** ایجاد یک فایل تنظیمات مرکزی مانند `src/config.py` و انتقال تمام این مقادیر به آنجا. سایر ماژول‌ها باید این مقادیر را از این فایل مرکزی بخوانند.
-   **اهمیت:** **بالا (High)**. متمرکزسازی تنظیمات، انعطاف‌پذیری پروژه را برای تست سناریوهای مختلف به شدت افزایش می‌دهد و نگهداری کد را ساده‌تر می‌کند.

### موضوع ۳: کد تکراری و عدم وجود ماژول‌های کاربردی (Code Duplication)

-   **مشکل:** برخی از قطعه کدها در ماژول‌های مختلف تکرار شده‌اند. به عنوان مثال، تابع تبدیل انواع داده‌ای `numpy` برای سریال‌سازی `JSON` که در `run_pipeline.py` تعریف شده بود، یک ابزار عمومی است و می‌تواند در بخش‌های دیگر نیز مورد نیاز باشد.
-   **راه‌حل:** ایجاد یک ماژول ابزار (`src/utils.py`) و انتقال تمام توابع عمومی و کاربردی به آن. این کار از تکرار کد جلوگیری کرده و استفاده مجدد از کد را ترویج می‌دهد.
-   **اهمیت:** **متوسط (Medium)**. این کار به پاکیزگی و سازماندهی بهتر کد کمک شایانی می‌کند.

### موضوع ۴: عدم وجود تست‌های واحد (Lack of Unit Tests)

-   **مشکل:** پروژه فاقد هرگونه تست خودکار (Unit Test) است. این بدان معناست که پس از ایجاد هر تغییر در کد، هیچ تضمینی وجود ندارد که عملکرد صحیح بخش‌های دیگر حفظ شده باشد. تمام تست‌ها به صورت دستی و با اجرای کل پایپ‌لاین انجام می‌شود که زمان‌بر و غیربهینه است.
-   **راه‌حل:** استفاده از یک فریمورک تست مانند `pytest` برای نوشتن تست‌های واحد برای هر ماژول. به عنوان مثال، برای `UniverseCreator` می‌توان یک تست نوشت که با داده‌های ساختگی، صحت عملکرد فیلترها را بررسی کند.
-   **اهمیت:** **بالا (High)**. وجود تست‌ها برای تضمین پایداری و کیفیت پروژه در بلندمدت ضروری است و امکان توسعه با اطمینان را فراهم می‌کند.

### موضوع ۵: مدیریت خطا و لاگ‌برداری (Error Handling & Logging)

-   **مشکل:** اگرچه لاگ‌برداری در پروژه وجود دارد، اما می‌توان آن را ساختاریافته‌تر و جامع‌تر کرد. در برخی قسمت‌ها، مدیریت خطاها می‌تواند بهبود یابد تا دلیل اصلی شکست پایپ‌لاین واضح‌تر مشخص شود.
-   **راه‌حل:** بازبینی کلیه بخش‌های پروژه و اطمینان از اینکه تمام خطاها به درستی مدیریت (try-except) و لاگ می‌شوند. می‌توان سطح لاگ‌ها را برای اطلاعات مختلف (Debug, Info, Warning, Error) بهینه‌تر تنظیم کرد.
-   **اهمیت:** **متوسط (Medium)**. لاگ‌های دقیق و مدیریت خطای خوب، فرآیند اشکال‌زدایی (Debugging) را بسیار سریع‌تر و کارآمدتر می‌کند.

---

## 3. ماتریس اولویت‌بندی (Prioritization Matrix)

برای درک بهتر اهمیت و فوریت هر موضوع، می‌توان آن‌ها را در یک ماتریس قرار داد:

| موضوع                                      | اهمیت (Importance) | فوریت (Urgency) | اولویت نهایی |
| ------------------------------------------ | ------------------- | ---------------- | ------------- |
| ۱. مدیریت نامناسب مخزن کد (Git)             | بسیار بالا         | بسیار بالا       | **انجام شد**  |
| ۲. پراکندگی تنظیمات (Configuration)        | بالا                | بالا             | **بالا**      |
| ۴. عدم وجود تست‌های واحد (Unit Tests)      | بالا                | متوسط           | **بالا**      |
| ۳. کد تکراری (Code Duplication)            | متوسط              | متوسط           | **متوسط**     |
| ۵. مدیریت خطا و لاگ‌برداری (Error/Logging) | متوسط              | پایین            | **متوسط**     |

**جمع‌بندی:** پس از رفع مشکل حیاتی مخزن `Git`، پیشنهاد می‌شود تمرکز بعدی بر روی **متمرکزسازی تنظیمات** و **نوشتن تست‌های واحد** قرار گیرد. این دو مورد بیشترین تأثیر را بر سلامت و کیفیت بلندمدت پروژه خواهند داشت.

---

## 4. تحلیل چرخه اجرای کد و ایرادات منطقی

بر اساس درخواست شما، چرخه اجرای کد و فایل‌های اصلی درگیر در پایپ‌لاین به همراه ایرادات منطقی یا نقاط قابل بهبود در هر یک، در ادامه تشریح می‌شود.

### تحلیل چرخه اجرا

پایپ‌لاین با فایل `run_pipeline.py` آغاز می‌شود. این اسکریپت به عنوان هماهنگ‌کننده اصلی عمل کرده و مجموعه‌ای از مراحل پردازش داده را به ترتیب مشخصی اجرا می‌کند. هر مرحله در کلاس مخصوص به خود در پوشه `src/` قرار دارد.

**جریان اجرا به شرح زیر است:**

1.  **`run_pipeline.py`**: کلاس `UniverseCreator` را فراخوانی می‌کند.
2.  **`src/universe_creator.py`**: تمام نمادهای بازار را دریافت کرده، آن‌ها را بر اساس قوانین ثابت (Hardcoded) نقدشوندگی و ارزش بازار فیلتر می‌کند و لیست نهایی را در فایل `cache/universe.json` ذخیره می‌کند.
3.  **`run_pipeline.py`**: کلاس `FullMarketDownloader` را فراخوانی می‌کند.
4.  **`src/full_market_downloader.py`**: فایل `cache/universe.json` را می‌خواند و به ازای هر نماد، تاریخچه کامل قیمت و حجم معاملات را دانلود کرده و در پوشه `data/full_market_data_csvs/` ذخیره می‌کند. این اسکریپت به صورت هوشمند از دانلود نمادهایی که از قبل وجود دارند، صرف‌نظر می‌کند.
5.  **`run_pipeline.py`**: کلاس `FullMarketFundamentalCollector` را فراخوانی می‌کند.
6.  **`src/full_market_fundamental_collector.py`**: مجدداً فایل `cache/universe.json` را می‌خواند. به ازای هر نماد، داده‌های بنیادی (مانند P/E و P/S) را دریافت کرده، پاک‌سازی داده (جایگزینی مقادیر خالی و مدیریت داده‌های پرت) را انجام می‌دهد و داده‌های نهایی و تمیز را در فایل `cache/master_fundamental_data.feather` ذخیره می‌کند. این اسکریپت همچنین یک "لیست سیاه" (Blacklist) از نمادهایی که دریافت اطلاعاتشان با خطا مواجه شده نگهداری می‌کند تا در اجراهای بعدی مجدداً برای آن‌ها تلاش نکند.

این توالی برای مراحل بعدی یعنی پیش‌پردازش، تست استراتژی و اعتبارسنجی نیز ادامه می‌یابد.

### ایرادات منطقی و بهبودهای پیشنهادی

بر اساس بررسی فایل‌های اولیه، در ادامه برخی از ایرادات و پیشنهادات کلیدی ارائه می‌شود:

#### ۱. `run_pipeline.py` (هماهنگ‌کننده)

*   **ایراد:** اسکریپت بسیار شکننده است. اگر هر مرحله با شکست مواجه شود، کل پایپ‌لاین متوقف می‌شود. یک سیستم مدیریت خطای جامع برای مدیریت این شکست‌ها وجود ندارد.
*   **ایراد:** وابستگی بین مراحل ضمنی است. به عنوان مثال، `FullMarketDownloader` به فایل `universe.json` نیاز دارد که توسط `UniverseCreator` ایجاد شده، اما این وابستگی تنها با خواندن کد قابل تشخیص است.
*   **پیشنهاد:** همانطور که در بخش قبلی تحلیل نیز ذکر شد، متمرکز کردن تنظیمات در `src/config.py` قابلیت نگهداری پروژه را بهبود می‌بخشد.

#### ۲. `src/universe_creator.py` (ایجاد جهان سرمایه‌گذاری)

*   **ایراد منطقی بالقوه:** معیارهای فیلتر کردن سهام (مانند `min_liquidity = 100,000` و `min_market_cap = 1,000,000,000,000`) به صورت **Hardcode** در کد ثبت شده‌اند. این یک **صلبیت منطقی بزرگ** است. اگر شرایط بازار تغییر کند، این مقادیر ثابت ممکن است سهام مناسب را حذف کرده یا سهام نامناسب را شامل شوند. این بخش هسته اصلی انتخاب سهام اولیه شماست و انعطاف‌پذیر نیست.
*   **پیشنهاد:** این مقادیر باید به فایل `src/config.py` منتقل شوند تا بتوان به راحتی و بدون تغییر کد، آن‌ها را تنظیم کرد و معیارهای مختلف را آزمایش نمود.

#### ۳. `src/full_market_downloader.py` (دانلودکننده قیمت/حجم)

*   **ایراد منطقی بالقوه:** اسکریپت با بررسی فایل‌های موجود در پوشه `data/full_market_data_csvs/`، وجود داده را کنترل می‌کند. اما **فقط وجود فایل را بررسی می‌کند، نه کامل بودن یا به‌روز بودن داده‌های داخل آن را**. اگر یک دانلود در گذشته ناقص انجام شده باشد، یک فایل ناقص باقی می‌ماند که اسکریپت به اشتباه آن را کامل فرض کرده و این امر منجر به ناقص بودن داده‌ها در مراحل بعدی می‌شود.
*   **پیشنهاد:** یک راهکار قوی‌تر، پیاده‌سازی یک استراتژی **به‌روزرسانی تدریجی (Incremental Update)** است. به جای بررسی صرف وجود فایل، اسکریپت می‌تواند تاریخ آخرین رکورد در هر فایل CSV را بررسی کرده و فقط داده‌های جدیدتر از آن تاریخ را دانلود کند. این کار اجراهای بعدی را بسیار سریع‌تر کرده و تضمین می‌کند که داده‌ها همیشه به‌روز هستند.

#### ۴. `src/full_market_fundamental_collector.py` (داده‌های بنیادی)

*   **نقطه قوت:** این اسکریپت نسبت به بقیه قوی‌تر است. از یک "کش قابل بازیابی" (`master_fundamental_data.feather`) و "لیست سیاه" (`failed_symbols.json`) استفاده می‌کند. این یک ویژگی عالی است، زیرا از دانلود مجدد داده برای همه نمادها در هر بار اجرا جلوگیری کرده و مانع از تلاش‌های مکرر برای دریافت داده از نمادهای مشکل‌ساز می‌شود.
*   **ایراد بالقوه:** اسکریپت مقادیر ناموجود `P/E` و `P/S` را با میانگین صنعت یا میانه کل داده‌ها جایگزین می‌کند. اگرچه این یک تکنیک استاندارد (Imputation) است، اما یک **فرض مهم** به شمار می‌رود. این یعنی شما برخی شرکت‌ها را "متوسط" فرض می‌کنید، در حالی که ممکن است ویژگی‌های منحصربه‌فرد (اما ناموجود) داشته باشند. این کار می‌تواند دقت تحلیل نهایی شما را برای آن سهام خاص کاهش دهد.
*   **پیشنهاد:** لاگ‌ها باید به وضوح مشخص کنند که چه تعداد مقدار برای هر ستون جایگزین شده است. این کار به شما یک "امتیاز کیفیت داده" می‌دهد و کمک می‌کند بفهمید چه میزان از داده‌های نهایی شما واقعی و چه میزان فرضی هستند (لاگ فعلی در این زمینه عملکرد خوبی دارد).

به طور خلاصه، نگرانی‌های اصلی منطقی شامل **قوانین فیلترینگ ثابت (Hardcoded)** در ماژول ایجاد جهان سرمایه‌گذاری و **عدم وجود مکانیزم به‌روزرسانی تدریجی واقعی** در دانلودکننده داده است که می‌تواند منجر به ناقص بودن داده‌ها شود.